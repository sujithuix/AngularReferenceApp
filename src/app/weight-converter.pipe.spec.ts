import { WeightConverterPipe } from './weight-converter.pipe';

describe('WeightConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new WeightConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
